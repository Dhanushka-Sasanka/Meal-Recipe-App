
export class Incredient{

    // we can also pass the parametters inside constructer without declare in the own class as 
    // the instance variables

    constructor(public name:string ,public amount:number){

    }

}